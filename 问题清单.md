# day07
## project46
- round(1.025,2)为什么等于1.02？round(1.035,2)为什么等于1.03？
- round(1.025,3)等于1.025，round(round(1.025,3),2)为什么等于1.02
- 为什么没有遵循银行家舍入算法？什么时候遵循银行家舍入算法？

# day08
## project47
- 为什么在该项目在pycharm运行可以正常运行，但是使用终端什么也没输出？
  - 当在程序中做输入输出时，有一个输入输出缓冲区。使用终端运行代码时，代码希望将hello输出到终端中，但是程序不会把hello给到终端，而是先把hello放到一个输出缓冲区，当输出缓冲区满足一定条件时，程序会把输出缓冲区的数据交给操作系统的输出缓冲区。
- 为什么要设置I/O缓冲区？没有I/O缓冲区会怎样？
  - 因为如果要做I/O操作输入输出的操作就会产生I/O中断，频繁的I/O中断会导致整个代码运转效率非常低下，CPU的利用率非常低。
- 是否了解现成的运行态、就绪态和阻塞态之间是怎么转移的？
- 当发生I/O中断时，线程会从哪种状态迁移到哪种状态？

# day10
## project56
- 为什么装饰器函数的格式是被装饰的函数是在装饰器函数中的一个函数中的？def wrapper()这个函数能不能删掉？

# day11
## project59
- lru_cache是什么？
- 它里面的maxsize参数是什么意思？
- 为什么我问豆包maxsize=3是否有同样的效果，它跟我说'复用优势消失了'？

## project60
- 创建类时，里面的方法的参数中为什么要写一个self？
- 为什么 **对象.方法()** 可以直接运行，而 **类.方法()** 要在括号里填写东西或者 **类().方法()** 才能运行？

## project61
- @classmethod是什么？有什么用？

## project62
- 为什么打印扑克里面的每张牌时，是

class Poker:
pass

```python
poker = Poker()
for card in poker.cards:
    print(card.show(),end='')
```
**card** 什么时候成为 **Card** 类的对象的？
- 后来我加上了(也可以使用将 **__str__** 换成 **__repr__** )
```python
def __str__(self):
    return self.show()
```
为什么可以直接删掉 **show()** 来实现同样的功能了？什么是魔术方法？它有什么用？

# day12
## project65
- **@staticmethod** 和 **@classmethod** 有什么区别？为什么在这个项目中只修改装饰器名就可以实现相同作用？

# day13
## project67
- **from enum import Enum** 是什么？有什么作用？用法是什么？

## project68
- 在 **from abc import ABCMeta, abstractmethod** 中 **ABCMeta** 类和 **abstractmethod** 分别什么作用？用法是什么？

# day17
## project81
- **type** 函数怎么判断出来日期数据的格式是<class 'datetime.datetime'>的

# day18
## project92
- 为什么添加好的水印也是旋转之后的？

# day20
## project98
- **quote** 把邮件的附件名进行百分号编码之后，QQ邮箱收到的邮件消息是百分号编码啊，把这一行删除之后，豆包说有的邮箱不能直接传输文字，怎么改能适配所有邮箱？加一个 **if-else** 吗？
- 为什么经过豆包改动后的代码要使用 **Application** ？这几行是干什么用的？听不懂豆包说的啥啊！！

# day22
## project105
- `scores = [[None] * len(courses)] * len(names)`和`scores = [[None] * len(courses) for _ in range(len(names))]`的区别？

# day22
## project109
- `items = items[:]` 和 `items = items` 为什么不一样？

